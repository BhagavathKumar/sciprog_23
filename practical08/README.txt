This code is for the week 8 practicals.

We calculate the Greatest Common Divisor of a pair of integers passed as argument, using two methods: euclidian algorithmn(iterative method), a recursive funtion using the formula.

To compile:

gcc gcd.c -o gcd
